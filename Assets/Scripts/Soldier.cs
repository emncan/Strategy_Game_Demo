using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Soldier : Singleton<Soldier>
{
    
    //public Point GridPosition { get; set; }
    //public Point spawnPoint;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    //private void OnMouseDown()
    //{
    //    if (Input.GetMouseButtonDown(1))
    //    {
    //        //Debug.Log(GameController.Instance.soldier.transform.parent.GetComponent<TileScript>().GridPosition.X);       
    //        //Debug.Log(GridPosition.X);
    //        LevelManager.Instance.start.GridPosition = new Point(GameController.Instance.soldier.transform.parent.GetComponent<TileScript>().GridPosition.X, GameController.Instance.soldier.transform.parent.GetComponent<TileScript>().GridPosition.Y);
            
    //    }

    //}
}
